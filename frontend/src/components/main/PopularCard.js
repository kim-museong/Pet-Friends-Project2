import styled from 'styled-components';

const PopularCardBox = styled.div`
  border: 1px solid rgb(186, 186, 186);
  padding: 10px;
  margin-top: 20px;
  margin-right: 20px;
  overflow: hidden;
`;

const PopularCard = () => {
  return <PopularCardBox>PopularCard</PopularCardBox>;
};

export default PopularCard;
